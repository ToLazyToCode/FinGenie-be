# Local Development Configuration
# Hardcoded values for local development (do not commit real secrets here)

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/fingenie
spring.datasource.username=fingenie
spring.datasource.password=fingenie
spring.datasource.driver-class-name=org.postgresql.Driver

# API Configuration
api-prefix=/api/v1

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=fingenie-redis-2024

# Kafka
spring.kafka.bootstrap-servers=localhost:9092

# AI Service
ai.service.url=http://localhost:8000

# JWT (hardcoded for local dev only)
jwt.secret=@local-dev-jwt-secret-key-fingenie-2026-not-for-production-use
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Mail (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hungphucvp7@gmail.com
spring.mail.password=pdfd jaed ytdh kvcv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Logging
logging.level.root=INFO
logging.level.fingenie.com.fingenie=DEBUG
logging.level.org.postgresql=DEBUG
logging.level.org.postgresql.jdbc=TRACE
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.hibernate.engine.transaction.internal.TransactionImpl=DEBUG
logging.level.org.springframework.boot.autoconfigure.jdbc=DEBUG
logging.level.com.zaxxer.hikari=DEBUG

# HikariCP connection pool debugging
spring.datasource.hikari.leak-detection-threshold=60000
