# PostgreSQL reference configuration.
# Runtime values are configured in application.properties and can be overridden via env vars.
#spring:
#  datasource:
#    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/fingenie}
#    username: ${SPRING_DATASOURCE_USERNAME:fingenie}
#    password: ${SPRING_DATASOURCE_PASSWORD:finpassword}
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
